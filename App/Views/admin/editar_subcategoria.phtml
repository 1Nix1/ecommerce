<!-- Content Wrapper -->
<div id="content-wrapper" class="d-flex flex-column">

    <!-- Main Content -->
    <div id="content">

        <!-- Begin Page Content -->
        <div class="container-fluid">
            <h1 class="h3 mb-2 text-gray-800 clearfix">Editar Subcategoria</h1>

            <?php
            if (isset($this->view->campos_obrigatorios) && $this->view->campos_obrigatorios == 'true') {
            ?>
                <p class="text-danger">Campos obrigatorios não preenchidos</p>
            <?php
            } else if ($this->view->campos_obrigatorios == 'false') {
            ?>
                <p class="text-success">Editado com sucesso</p>
            <?php
            }
            ?>
            <form action="/admin/edita_subcategoria" method="POST">
                <div class="form-row">
                    <div class="form-group col-md-3">
                        <input name="id" value="<?= $this->view->getEditSubcategoria['id'] ?>" type="hidden" required>
                        <label for="nome">Nome</label>
                        <input name="nome" value="<?= $this->view->edita_subcategoria['nome'] ?>" type="text" class="form-control" id="nome-produto" placeholder="Nome" required>
                    </div>
                    
                    <div class="form-group col-md-3">
                        <p>Categoria</p>
                        <select name="categoria" class="custom-select resultado select-categoria-sub" required>
                            <option selected>--</option>
                            <?php
                            foreach ($this->view->categorias as $idx_sub => $categoria) {
                            ?>
                                <option value="<?= $categoria['id']; ?>" <?php echo $this->view->edita_subcategoria['id_categoria'] == $categoria['id'] ? 'selected' : ''; ?>><?= $categoria['nome']; ?></option>
                            <?php } ?>
                        </select>
                    </div>
                    <div class="form-group col-md-3">
                        <p>Status</p>
                        <div class="switch__container">
                            <input id="switch-shadow" name="ativado" <?php echo $this->view->edita_subcategoria['status'] == 'ativo' ? 'checked' : ''; ?> class="switch switch--shadow" type="checkbox" />
                            <label for="switch-shadow"></label>
                        </div>
                    </div>
                    
                </div>
                <button type="submit" name="enviar-formulario" class="btn btn-primary mt-3">Salvar</button>
                <a type="submit" href="/admin/subcategorias" class="btn btn-secondary mt-3">Cancelar</a>
            </form>
        </div>
        <!-- End begin page content -->
    </div>
    <!-- End of Content Wrapper -->